---
'@eigenpal/docx-editor-core': patch
---

Legacy form fields keep their run formatting (font, size, bold) when a value is set, a checkbox that gains w:default stays in schema order, pasted HTML can no longer carry arbitrary field or w:sdtPr markup into the saved document, and suggestion mode no longer turns collaborators' incoming Yjs edits into tracked insertions.
